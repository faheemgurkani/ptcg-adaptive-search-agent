# Phase 4 holdout analysis — search depth

Sweep in progress. Per-matchup rates, time/candidate curves, and the knee will appear here after `scripts/analyze_phase4_results.py` runs on a completed (or partial) `phase4_holdout_summary_latest.json`.

Phase 3 anchors (not re-run): V1 **90.0%**; V2 at 0.3 s / 8 candidates **36.2%**.
